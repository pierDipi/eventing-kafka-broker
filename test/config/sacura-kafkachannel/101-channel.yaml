apiVersion: messaging.knative.dev/v1beta1
kind: KafkaChannel
metadata:
  name: channel
  namespace: sacura-kafkachannel
spec:
  numPartitions: 10
  replicationFactor: 3

  delivery:
    retry: 5
    backoffPolicy: exponential
    backoffDelay: PT5S
